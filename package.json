{
  "name": "rfq-draft-order",
  "version": "1.0.0",
  "description": "Shopify RFQ Draft Order Proxy",
  "main": "server.js",
  "type": "module",              // ✅ keep this if you are using ES Modules (import/export)
  "scripts": {
    "start": "node server.js",   // ✅ this is what Render will run
    "dev": "nodemon server.js",  // (optional) for local development
    "build": "echo 'Nothing to build'" // ✅ prevents Render from failing if it expects a build step
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "dotenv": "^16.4.5"
  }
}
